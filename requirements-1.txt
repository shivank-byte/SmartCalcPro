# SmartCalc Pro uses only the Python standard library:
# - tkinter (GUI)
# - math, statistics (calculations)
# - datetime (age calculator, history timestamps)
#
# No external packages required. This file is here for completeness /
# in case you extend the project later (e.g. matplotlib for graphing).
